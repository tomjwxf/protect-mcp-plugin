Verify the following Ed25519-signed receipt or run the self-test:

```bash
npx @veritasacta/verify $ARGUMENTS
```

If no arguments provided, run the self-test:

```bash
npx @veritasacta/verify --self-test
```

Show the verification result and explain what was checked.
