Wrap the following MCP server command in protect-mcp shadow mode (log all tool calls, block nothing):

```bash
npx protect-mcp -- $ARGUMENTS
```

Explain what shadow mode does and show the user how to view the logs.
